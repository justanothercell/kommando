fn main() { // main
    printf("hello, world\n");
    let n = 5;
    printf("%dth fibonacci number: %d\n", n, fib(n));
}

fn fib(n: i32) -> i32 {
    return if n < 2 {
        return n;
    } else {
        fib(n - 1) + fib(n - 2)
    };
}

/* block comment */