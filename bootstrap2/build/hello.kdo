fn printf(fmt: ::std::c_const_str_ptr, *) -> ::std::i32;

fn main() {
    let fmt: ::std::c_const_str_ptr = "hello %d\n";
    printf(fmt, 42);
    printf("10th fib: %u\n", fib(10u32));
}

fn fib(n: ::std::u32) -> ::std::u32 {
    if n < 2u32 { n }
    else { fib(n - 1u32) + fib(n - 2u32) }
}